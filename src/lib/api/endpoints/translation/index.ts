import Translation from "./Translation";
import Locales from "./Locales";

const translationEndpoints = {
  translation: Translation,
};

export { Translation };
export { Locales };
export default translationEndpoints;
